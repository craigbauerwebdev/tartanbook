import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // change to node-sass - Sass
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
